# xc6slx9.ocd
# OpenOCD commands

# impact->boundary scan->right click in right window->add xilinx device glue.bit->one step svf

source [find interface/altera-usb-blaster.cfg]

telnet_port 4444
gdb_port 3333

# JTAG TAPs
jtag newtap chip tap -expected-id 0x24001093 -irlen 6

init
scan_chain
